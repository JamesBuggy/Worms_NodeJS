module.exports = {
    use_https: false, // Enable/disable https
    port: 3002, // Default port used by the game server (It will scan for another port if this one is in use)
    masterHost: "localhost", // Host address of the master server
    masterPort: 3001,
    apiHost: "localhost", // Host address of the rest api
    apiPort: 3000
}